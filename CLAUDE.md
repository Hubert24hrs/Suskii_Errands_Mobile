# CLAUDE.md — Suskii Errands

The single source of truth for this project is **`docs/spec/SUSKII_BUILD_PROMPT.json`**. Read it in full before starting any work.

## Your role

Claude Code is the **backend agent and technical lead**. Follow `master_spec` + `agent_claude_code`. Kimi Code builds every frontend (`agent_kimi_code`); treat that section as context only.

## Ownership

| Path | Owner |
|---|---|
| `supabase/` (migrations, functions, tests, seed), `services/` (ai, voice-agent, workers), `contracts/` (except `draft/`), `infra/`, `.github/workflows/backend-*.yaml`, `docs/adr/`, `docs/runbooks/`, `docs/research/`, `docs/audit/`, `docs/OPEN-DECISIONS.md`, `CLAUDE.md` | Claude Code |
| `apps/` (mobile, web-customer, web-marketing, web-admin), `packages/`, `contracts/draft/`, `contracts/CHANGE_REQUESTS.md`, `AGENTS.md`, `.github/workflows/frontend-ci.yaml` | Kimi Code. **Do not edit.** Audit and file findings in `HANDOFF.md` instead |
| `HANDOFF.md`, `README.md` | Shared. Append; never rewrite the other agent's entries |

## Working alongside Kimi Code

Kimi edits **this same working tree at the same time**. Untracked files that are not yours will appear mid-session; leave them alone.

- **Never `git checkout` or `git switch` here** — it moves `HEAD` under Kimi, and Kimi's next commit lands on your branch.
- Commit from a throwaway worktree instead: `git worktree add -b <branch> <scratch>/wt origin/main`, copy in your paths, commit, push, `git worktree remove`.
- Stage only Claude-owned paths. Do not commit `HANDOFF.md`, `apps/`, `packages/` or Kimi's config; edit `HANDOFF.md` in place and let Kimi commit it.
- Remote: `https://github.com/Hubert24hrs/Suskii_Errands_Mobile`. Phase 0 and the backend docs are merged into `main` (fast-forward, 2026-09-16); the working branch was deleted, so `main` is the only branch. Work from a fresh worktree branch off `origin/main` each time.
- After merging, fast-forward the shared tree with `git reset --mixed origin/main` rather than `git pull`: it moves the ref and the index without rewriting files Kimi may be editing.

## Status

| | |
|---|---|
| Phase 0 — Deep research | **Complete.** See `docs/research/CHECKPOINT-PHASE-0.md` |
| Phase 1 — Planning, contracts v1 | **Stage A complete** (`docs/plan/`, 2026-09-16): state machines, ERD, RLS matrix, money flows, C4, threat model, data flow, AI design, infra/CI-CD, test strategy, PRD (147 stories), timeline. Stage B (contracts v1 + fixtures) waits for M8.5 |
| Spikes | S-06, S-10, S-13, S-14 passed. The rest need credentials or devices — `docs/research/spikes/README.md` |
| Kimi | M1 + M2 committed (`e473c9b`). Follow-up 2026-09-17: all findings through M3.10 fixed and verified; package suites green (94 tests). Kimi reported the M3 foundation done 2026-09-17; M2 screens now pass idempotency keys. Whole-repo audit 2026-09-17 (`docs/audit/AUDIT-2026-09-17.md`): package suites green (94), analyzer clean; **M3.15 (High) sign-in never left `/auth` — fixed and committed (`e5c2f6f`, `ref.watch` in `router.dart`) at the user's request, one line only**; M3 screens do not compile yet (mid-edit). M3.18 (High) fixed (`46cd262`): `frontend-ci.yaml` had failed on every run since M2 (`build_runner` in `apps/mobile`); it now passes, so the analyzer and package tests gate frontend commits. **M3 complete and committed 2026-09-18 (`0559988`)** — request form, concierge text + voice, offers board, request detail. Reviewed: format, analyzer and 94 package tests green, screens match the PRD/AI/offer rules, keys held per intent. Two bugs found by running a web build and **fixed in that commit at the user's request**: M3.19 (High, web only) `newIdempotencyKey()` threw under dart2js (`1 << 32` is `0`), so every keyed action failed on web and the concierge never opened; M3.20 (Medium) a hardcoded `/100` price prefill corrupted amounts in zero-exponent currencies (UGX). `frontend-ci` green on the merge. Open: M3.16 (country list), M3.14 (residual inline keys in M2 screens), M3.6 (contract), M3.12–M3.13 low, V.1–V.5 (preview); `frontend-ci` now also runs `dart test -p chrome` for `suskii_core` and `suskii_domain` (`aade70e`), and runs on pull requests that touch the workflow itself — the pre-fix key generator fails that run and passes on the VM, which is what let M3.19 through |
| Phase 2 — Backend foundation | **Started 2026-09-16** before contracts v1 (ADR-0013, user-approved). Done and green in CI: 11 migrations, 148 pgTAP assertions (`backend-db.yaml`); Edge Functions `auth-send-sms`, `device-integrity` (Play Integrity + App Attest with receipt verification) and `health`, 59 Deno tests (`backend-functions.yaml`). Sessions: `list_sessions` / `revoke_session` / `revoke_other_sessions` with an end-to-end test against real GoTrue. Storage: private `avatars` and `kyc-docs` buckets, one folder per user, `kyc-docs` write-only for clients; end-to-end against the real Storage API. Observability: `health` function + DB health checks + Sentry-ready instrumentation. Terraform for GCP in `infra/terraform` (validated, not applied; `infra.yaml` CI). Deploy pipelines: `pipeline-main` (checks → dev → staging), `deploy-production`, `rollback-functions`, `infra-deploy`; runbook RB-14. Security CI: `security.yaml` (project policy checks, gitleaks, osv-scanner; weekly). Phase 2 runbooks written: RB-01, RB-07, RB-09, RB-10, RB-13, RB-14 (none rehearsed yet). Backup worker `services/workers` (`suskii-backup dump|verify|storage-sync`, Cloud Run Jobs in Terraform, `services-workers.yaml` CI). iOS App Attest verification (2026-09-17: attestation + assertion, tested against Apple's sample and a real device capture; migration `20260917120000`). Google/Apple sign-in wired into deploy config (per-environment variables). Open: session lifetime (OD-22), SMS vendor adapters (S-09), App Attest fraud-metric refresh (needs a DeviceCheck key; Authorization header format unconfirmed), Cloudflare/Sentry Terraform (after domain decision I-2). Needs GCP billing + a Supabase org for deployed environments |
| Phase 3 — Marketplace core | **Complete except where money, vendors or a client decision block it** (started 2026-09-18 under ADR-0014; 11 migrations, `…120000`–`…121000`). Against the spec's own task list: service taxonomy and custom services ✅ (**embeddings not built** — ADR-0006 must pick the model before a vector dimension can be fixed, and a guessed dimension means a re-embed migration later); requests ✅; PostGIS matching **and the notification fan-out** ✅; offers and negotiation ✅; job state machine ✅; realtime private channels **and database broadcasts** ✅; chat with moderation hooks and storage buckets ✅; favourites, blocks, reports, two-way ratings and the reputation job ✅; business accounts, members, dispatch, vehicles and zone rules ✅. Also shipped: proofs and the marketplace buckets. 21 pgTAP files. Blocked, not forgotten: payment, settlement, refunds, disputes and cancellation fees (OD-06/OD-08/OD-19); calls and masked numbers (vendor); push delivery (the outbox carries the job, nothing sends it yet); zone polygons (client gazette data); guardrail calibration (OD-23); business and worker verification (Phase 4) |
| Phase 4 — Verification & safety | **Complete except the two vendor seams** (2026-09-21, ADR-0013/ADR-0014 terms). Safety (`…120000_safety`): `trusted_contacts` (five, by trigger; numbers encrypted per ADR-0007), `sos_incidents` + `sos_partners` with the ops queue verbs and a three-minute escalation (AD-21), and `trip_share_links` whose token is stored only as a hash, with `get_shared_trip` as the second and last `anon`-callable function — 40 assertions. KYC (`…120100_kyc`): the `kyc` schema no client role can reach, verification sessions and steps, the Verification Officer queue with a no-self-review rule, blind-indexed identity documents (one identity, one account), police clearances, `audit.kyc_access` logged before any URL exists, and document expiry that lapses the standing with it — 41 assertions. Moderation (`…120200_moderation`): `prohibited_items` and `moderation_cases`, deterministic rules that a model cannot overrule, fail open per OD-21, `block` the only refusal (`ERR_CONTENT_NOT_ALLOWED`), upholding takes content down and never touches the account — 47 assertions. Risk (`…120300_risk`): `fraud_flags`, immediate signals (mock location, failed device integrity, self-dealing across one handset) and an hourly sweep (device reuse, collusive pairs, velocity, cancellation abuse), with a review queue that **bans nobody** — 36 assertions. New: OD-24 (counsel must sign the restricted lists; every rule is `[A]`) and OD-25 (what follows a confirmed flag is the client's policy, not ours). Blocked, unchanged: the identity vendor (Smile ID, OD-13), the SOS partner (client action 12) and chargeback rules (no gateway until Phase 5) — seams, not integrations |
| Phase 6 — Communication | **The database half is built** (2026-09-21), the two vendor halves are not. Calls (`…120500_calls`): `calls` and `masked_numbers`, `start_call` / `answer_call` / `end_call` / `request_pstn_fallback`, a one-minute ring timeout, and the missed-call system message and push (SH-15). **No recording column exists and none may be added** — the spec says metadata only and the DPIA was written on that, so `25_calls_test` asserts it structurally. The server picks the callee from the job, the call window is `chat_is_open` rather than a second copy of the same rule, and simultaneous attempts resolve into one call instead of being refused (so `ERR_CALL_IN_PROGRESS` is now reserved and never raised). Delivery (`…120600_notification_delivery`): `notification_channels` with per-channel preferences and quiet hours, job-critical kinds never suppressed (SH-16), the decision carried on the outbox event so no sender re-derives it; `profiles.timezone` added and client-writable, because quiet hours without a time zone are a guess. Scheduled errands (`…120700_scheduled_requests`): drafts publish themselves at a configured lead time through `private.do_publish_request`, the one path `publish_request` now also uses. 66 assertions across two files. **Not built:** the LiveKit access-token Edge Function (needs the project's API key and secret — no LiveKit account exists, REPORT §6.1) and VoIP push via APNs/FCM (needs the credentials and a device lab, S-03). PSTN masking is a seam: no telephony provider is contracted (REPORT §6.3), so `masked_numbers` ships empty and `request_pstn_fallback` returns NULL while recording the demand — it returns rather than raises because an exception would roll that record back, which is what CI caught |
| Phase 8 — Admin backend | **Started 2026-09-21.** Support tickets (`…120800_support_and_admin`): `support_tickets`, `ticket_messages` with internal staff notes, `open_ticket` / `reply_to_ticket` / `ticket_queue` / `update_ticket`. The point of them is `private.support_ticket_scope`, which closes audit finding **S.1**: the Phase 3 policies gave every support agent and dispute officer blanket read of every conversation, message and call, where the RLS matrix says support reads a job's chat only when a ticket names it — a control the DPIA relies on. Support is now scoped to ticketed jobs and `dispute_officer` loses the access until `disputes` exists (Phase 5). Also shipped: `suspend_provider` / `reinstate_provider`, the explicit, bounded, audited decision OD-25 keeps out of the risk engine — an end date is required, so a permanent ban is not something one function call can do. 38 assertions. Part 2 (`…20260922120000_admin_config`): `config_changes`, with `propose_config_change` / `review_config_change` / `config_change_queue` / `country_pack_readiness`. `countries`, `feature_flags` and `remote_config` have existed since Phase 2 with **no write path at all**, and the fix was not an UPDATE policy: a proposal records what somebody wants to be true and what is true now, and only the last approval applies it. Two signatures for a commission or referral rate, for a country going live and for a referral campaign; one for everything else. A proposal is a **diff**, so a field it does not name is left alone. `private.country_pack_gaps` makes the spec's one-line rule mechanical — a country goes live only when its pack is complete — and a pack whose only payment route is the development provider counts as incomplete. A refused apply leaves the change `pending`, never `applied`. Part 3 (`…20260922120300_analytics`): the `analytics` schema, sixteen daily views, `analytics.exports` as a cursor and `public.analytics_report` as the one vetted door for dashboards. **Aggregates only** — not one column names a person, asserted against `information_schema`, which is the cheapest way to satisfy the pseudonymisation data-flow row 24 requires; per-user cohorts need a keyed pseudonym whose key belongs in KMS (ADR-0007), so that is the same seam as the payout decryption. A failed export records the failure and **does not move the cursor**. 52 assertions across parts 2 and 3. **Audit S.1 is closed** (`…20260922120200_scoped_reads`): `job_events`, `chat-media`, `request-media` and `job-proofs` scope by ticket or by case, super admin keeps the unscoped read the matrix gives it, and the `receipts` bucket the matrix named but nothing had built is created with its policies — `submit_float_receipt` had been validating a path into a bucket that did not exist. 26 assertions. Part 4 (`…20260922120600_admin_people`) closes the rest: `business_verification_queue` and `decide_business_verification` — **`organizations.verification_status` has existed since Phase 3 with nothing behind it**, so every business on the platform was `unverified` for ever; `suspend_organization` / `reinstate_organization`, enforced by replacing `private.is_active_provider` rather than by adding a check to each caller that might remember; and `admin_user_search` / `admin_user_summary` / `admin_organization_summary` / `admin_revoke_user_sessions`. Looking somebody up writes an audit row before it returns anything, and search is by exact id, phone suffix or name **prefix** — a substring match on every name is a directory dump. 41 assertions with the trip trail |
| Phase 5 — Money | **Started 2026-09-21. The ledger is built; the gateways are not.** `…120900_ledger`: `ledger.accounts`, `transactions`, `entries` and `balances` with the 13-account chart from ADR-0011, `ledger.post` as the only way money is written down, `ledger.reconcile()` nightly through `private.record_health_check`, and `public.my_balances()`. Signed minor units, one currency per transaction, balances derived. Per S-14: `post` validates that the entries balance **before** writing and raises a catchable `P0001`, because the deferred constraint trigger fires outside any exception handler and its abort takes the whole transaction with it — the trigger stays as the invariant nothing can route around. 30 assertions, whose spine is the spec's own worked example posted through money-flows 1a–1d: 12.50 commission, 84.60 to the provider after the fee they bear, 2.19 referral the platform funds, net platform revenue of exactly 10.31. **This was buildable all along and I had called it blocked** — OD-06 is titled "commission rate *typo*" in the spec and resolves to 12.5%, already `verified` in the NG pack. Part 2 (`…121000_payments`): `payments`, partitioned `webhook_events`, `start_payment` (transition 7, and the money snapshot — rate, commission and net written once), `private.ingest_webhook` (raw first, de-duplicated across partitions by an explicit check because a partitioned unique index must carry the partition key), `private.confirm_payment` (transition 8 and posting 1a), `record_gateway_settlement` (1b), `recognise_earnings` + an hourly sweep (transition 18 and posting 1c, after the dispute window), and `expire_payments`. **A client cannot confirm its own payment** — `confirm_payment` has no grant to any client role, and the test asserts it. 42 assertions. Still blocked: the gateway itself, payouts and withdrawals (merchant accounts, client action 4); Part 3 (`…121100_refunds_and_cancellations`): `refunds`, `cancel_job` for the paid half of cancellation, and `execute_refund`/`fail_refund` as the gateway seam — money-flows 5a, 5b and 5c. Who cancels and when decides who pays: nothing before the provider is `en_route`, `cancellation_fee_bps` plus the gateway fee after (OD-08, OD-19, both remote-config), and always a full refund when the provider is the one cancelling. A failed refund posts nothing, because the platform still owes the money and the books should say so. 31 assertions. Part 4 (`…121200_promos_and_tips`): `promo_codes` (column-level read — the budget is not granted, because knowing it tells a customer when to hurry), `promo_redemptions`, `tips`, `preview_promo`, `add_tip`, and `payments.kind`/`discount_minor`. `start_payment` was **dropped and recreated** with a fourth argument rather than overloaded, since two overloads with defaults would make the two-argument call ambiguous. Postings 4a, 4b and 2: a promo never reduces provider earnings — commission stays on the full negotiated price and the discount is `platform_promo_expense` — and a tip carries no commission. 33 assertions. Part 5 (`…121300_item_float`): the goods money as its own line — `submit_float_receipt`, `approve_float_receipt`, `private.release_item_float` and an auto-approval sweep, with `payments.job_amount_minor`/`float_minor`/`float_surcharge_minor` saying how a charge divides. Postings 3a, 3b, 3c: no commission on groceries, the customer covers the gateway's fee on the float so the provider is reimbursed exactly what they spent (OD-04), and the unspent remainder becomes a refund with its own reason. **Four existing functions had to learn about it** — `start_payment`, `confirm_payment`, `recognise_earnings` and `cancel_job` each assumed a payment was one amount — so all four are replaced here. 28 assertions. **Phase 5 is now complete except what needs a gateway**: payouts, withdrawals and the Flutterwave/Paystack integrations themselves. Part 6 (`…121400_disputes`): `disputes` and `dispute_evidence`, `open_dispute` / `submit_dispute_evidence` / `withdraw_dispute` / `dispute_queue` / `assign_dispute` / `resolve_dispute`, and `private.dispute_scope` — which gives `dispute_officer` back the scoped read it lost in Phase 8's slice, closing the other half of audit finding **S.1**. Opening one freezes settlement; `frozen_from` records the status so withdrawing restores it exactly rather than assuming `confirmed` and releasing money for unfinished work. Posting 6a: commission on what is kept, the provider still bearing the collection fee. **The refund total is checked here catchably** — a partial refund decided by a person is the first thing that can breach the `refunds` invariant, whose deferred trigger cannot be caught (S-14). Nobody adjudicates a job they were on, tested with a provider who is also an officer. 38 assertions. Part 7 (`…121500_payouts_and_withdrawals`): `payout_accounts` (ciphertext + blind index per ADR-0007, and the ciphertext granted to nobody), `payouts` with an explicit state machine because a transfer can reverse hours later, `withdrawals` with the country pack's one- and two-approver thresholds through the existing four-eyes `approvals` table, and `record_chargeback`. Postings 1d, 7, 8 and 9. A shared bank account is **flagged, not refused** — two people may genuinely share one, and refusing would strand somebody with no way to be paid. 44 assertions. **Phase 5's database half is complete**; what remains is the Flutterwave/Paystack HTTP calls themselves, which need merchant accounts (client action 4). `private.create_payout` emits the instruction and `private.record_payout_result` takes the answer back. Parts 8 and 9 (`…121600_outbox_dispatch`, `…121700_gateway_rpc`) plus two Edge Functions close that seam: `private.claim_outbox` with `FOR UPDATE SKIP LOCKED` (the outbox has been written to since Phase 2 and read by nobody), a `gateway_*` surface in `public` granted to `service_role` alone because PostgREST cannot reach `private`, `payments-webhook` (verify the signature over the raw body → store → de-duplicate → verify server-side → ledger) and `payments-worker` (checkout, transfer, name enquiry). 88 Deno tests. **The Flutterwave and Paystack adapters are deliberately unwritten** — their payload shapes and signature schemes are not in the research, and S-12 settles them against a sandbox that needs merchant accounts. The console provider refuses to run in production, so a country pack still routing to it fails loudly. Part 10 (`…20260922120100_referrals`) is the last feature: `referral_codes`, `referrals`, `referral_commissions` and `referral_campaigns`, with `my_referral_code` / `claim_referral_code` / `my_referrals` / `my_referral_summary`, `private.accrue_referrals`, `private.reverse_referrals` and an hourly maturing sweep. The ledger has carried `referral_earnings` and `platform_referral_expense` since part 1 with nobody on the other end; this is the other end, and the spine of its 54 assertions is the spec's own 2.19. The five states each sit somewhere real — `pending` at payment, `earned` at confirmation, `holding` when earnings are recognised (**where the ledger lines are written**, because money-flows 1c puts them there), `available` after the fraud hold, `reversed` on a chargeback — and `private.available_minor` learns to subtract what is posted but still holding, or the hold would be a label with nothing behind it. `recognise_earnings`, `resolve_dispute` and `record_chargeback` are **replaced whole** to carry postings 1c, 6a and 7 as money-flows writes them; 6a *recalculates* rather than reverses, because the job still happened. One referrer per referee by UNIQUE, which is what makes single-level structural. A self-referral across two accounts on one handset is **accepted and silently blocked**: the row is kept because the record is the point, and telling somebody which signal caught them is a free oracle. OD-01, OD-02 and OD-03 are all open and all therefore configuration — a client answer moves a row. **Phase 5 is complete except the vendor HTTP calls** |
| Phase 9 — Audit and hardening | **First pass done 2026-09-22** (`docs/audit/AUDIT-2026-09-22.md`). Scope: everything since the 2026-09-21 audit — 6,457 lines across thirteen migrations plus the two payment functions. Four mechanical sweeps (idempotency, the S.2 raise-after-record class, the S.2b CASE-to-enum class, grants and RLS) found nothing; reading the money paths against `money-flows.md` found five defects, **two of them High and neither visible to a test, a reviewer or a user until the day it cost something**. T.1: `webhook_events` partitions were created once and never again, so about three months after a deploy every payment would stop being confirmed while every health check stayed green — fixed, and the partition check now looks at every partitioned table rather than `audit.log` alone. T.2: three functions create a refund and one emitted the event that pays it, and the worker **marked that event complete unread** — fixed by making the emit a trigger on the table, giving the worker a refund branch and `PaymentProvider` a `createRefund`. T.3: a chargeback before settlement reversed postings that were never made, inventing an 84.60 debt against a provider — fixed by asking the ledger whether earnings were recognised. T.4: `stacks_with_referral` was configuration nothing read. T.5: the analytics health check called a never-run exporter stale. Also built here: `location_samples` (`…20260922120500`), the trail the RLS matrix has had a row for since Phase 1 — written **only while the provider is on a job**, read by the customer only after it ends, by a dispute officer only when a case names it, by support **never**, and dropped by partition at `location_retention_days`. 63 assertions across two files. Open and noted rather than fixed: four creates without an idempotency key (N.1). **Second pass, the RLS surface** (`docs/audit/AUDIT-2026-09-22b.md`): 64 tables, 107 client-callable functions, 14 storage policies. Five mechanical sweeps came back clean — every table has RLS and FORCE, only `get_bootstrap` and `get_shared_trip` reach `anon`, and all 14 functions the authorisation heuristic flagged were false positives, each listed so the next pass does not re-flag them. Reading the matrix row by row found four. **U.1 is the pass**: the matrix defines `R:scope` as the admin's country scope and carries it on fifty-odd cells, `admin_users.country_scope` has existed since Phase 2 to hold it, and **one function read it** — so a support agent scoped to Nigeria read four other countries and a finance officer read every country's money. Twenty-eight policies scope through three helpers now; `super_admin` is never scoped, it fails closed, and `fraud_flags` and `moderation_cases` are listed rather than half-done because their subjects are polymorphic. U.2: a business could rename itself after verification, which the matrix has always forbidden. U.3: `get_provider_card()` is named in the matrix and **did not exist**, so a customer comparing offers could learn nothing about the providers and the offers board had no backend — a hole in the product made by a correct security decision with its counterpart missing. U.4: the column-grant assertion covered two of five tables. Also built: the outbox's second reader (`…20260922120900` + `notifications-worker`), closing N.3's notification half — it re-decides nothing, records an outcome per channel, retries only when every channel failed, and **enforces the push payload rule in code** so an amount or a name never reaches a lock screen. 29 more assertions and 12 more Deno tests |
| Next Claude work | Phases 2 to 6 and 8 are built and Phase 9 has had two passes — the money (`AUDIT-2026-09-22.md`, five defects) and the RLS surface (`AUDIT-2026-09-22b.md`, four), all fixed. Buildable now: the country scope on `fraud_flags` and `moderation_cases` (U.1's remainder, needs a switch over polymorphic subjects), and Phase 7's AI layer as far as it goes without Vertex. Blocked on the client, not on code: every gateway integration (action 4), LiveKit and telephony (action 11), Phase 7's Cloud Run service (action 1 — GCP billing), the identity vendor (action 7), the SOS partner (action 12), the device lab (action 6). **The client action list is overdue**: W0 was the week of 14 Sep 2026 and actions 1–9 were all due to start by now. Deploy pipelines stay switched off until the GCP projects, Supabase org and GitHub environments exist (RB-14). Launch base case: week of 7 Jun 2027 |


Phases 0 and 1 produce documents only. Spike code is throwaway: `spike/*` branches, under `spikes/`, never in `apps/`, `supabase/` or `services/`. From Phase 2, `supabase/` holds production code — see `supabase/README.md` for its rules.

## Document map

| File | What it is for |
|---|---|
| `docs/OPEN-DECISIONS.md` | OD-01…OD-22 awaiting the client or counsel. Quote it in every checkpoint |
| `docs/adr/` | Architecture decision records, indexed in `docs/adr/README.md` |
| `docs/research/` | Phase 0 evidence: report, vendor matrix, country packs, compliance and DPIA, risks, cost model, spikes |
| `docs/plan/` | Phase 1: state machines, UI draft review, architecture, AI design, infra, test strategy, PRD (`prd/`), timeline with the client action list |
| `docs/runbooks/` | Operational procedures, one per failure mode |
| `docs/audit/` | Dated audit findings after each integration milestone; whole-repository audit 2026-09-17; Phase 5 and Phase 8 money audit 2026-09-22; RLS surface audit 2026-09-22 (second pass) |
| `contracts/README.md` | What contracts v1 will contain and the rules that bind both agents |
| `contracts/v1-preview/` | Non-binding preview pinned early: error codes, Auth error mapping, enums (generated), `Money` shape; CI drift check `contracts.yaml` |

## Maintenance policy — keep these current

Documentation is part of the definition of done, not a postscript. Update on the trigger, in the same change:

| When this happens | Update |
|---|---|
| A major technical decision is made | New ADR in `docs/adr/` + a row in its index. Never edit an accepted ADR; supersede it |
| A spike finishes | `docs/research/spikes/S-xx-results.md`; flip any ADR that was `Proposed` on it; adjust the risk register |
| The client or counsel answers an open decision | `docs/OPEN-DECISIONS.md` (status, date, answer, implementing ADR), and the ADR itself |
| A phase ends | Phase checkpoint file + `HANDOFF.md` entry + this file's Status table; re-list unresolved ODs |
| A vendor price, API or country rule changes | `docs/research/REPORT.md` (with the source), `cost-model.md`, the affected `country-packs/*.yaml` field and its `_status` |
| A new risk appears or one changes score | `docs/research/risk-register.md` |
| A feature is built | Its runbook, contract entry, and the commands section below. A new or removed `ERR_` code updates `contracts/v1-preview/error-codes.json`; a new enum value regenerates `enums.json` (`python contracts/tools/check_preview.py . --write`) |
| An audit runs | `docs/audit/AUDIT-<date>.md` + findings for Kimi in `HANDOFF.md` |
| Contracts change | `contracts/CHANGELOG.md` (semver) + a migration note in `HANDOFF.md` for breaking changes |

Two rules: never delete a decision or a finding, supersede it; and every factual claim carries its evidence tag — `[V]` verified from an official source, `[S]` secondary, `[A]` assumption. Nothing tagged `[A]` may be implemented as fact.

## Non-negotiables (from the spec)

- The server is the source of truth for prices, money, job states, verification and permissions.
- Money is integer minor units + ISO 4217 code (use the exponent table: UGX = 0). Never use floats.
- All state and money writes go through database functions or Edge Functions. RLS on every exposed table with pgTAP allow/deny tests.
- `SECURITY DEFINER` functions use an empty `search_path`, check `auth.uid()` and role/mode, and validate inputs.
- Idempotency keys on every create, transition and money operation. Webhooks: verify signature, store raw, de-duplicate, then verify server-side.
- No service-role key in any client or repo. Secrets live in Supabase Vault or GCP Secret Manager. Don't use pgsodium (ADR-0007).
- Held funds are "held", never "escrow" (ADR-0002).
- Never fabricate vendor APIs, model names or legal requirements. Mark uncertain items `VERIFY-IN-RESEARCH`.
- Write comments only for non-obvious logic.

## Commands

Backend (details and rules in `supabase/README.md`; CLI pinned to 2.117.0):

```bash
npx supabase@2.117.0 start                         # needs Docker
npx supabase@2.117.0 db reset --local              # all migrations + dev seed from zero
npx supabase@2.117.0 test db --local supabase/tests/database  # pgTAP
npx supabase@2.117.0 db advisors --local --type all --level warn --fail-on warn
bash supabase/local-fallback/run-plain-postgres.sh # no-Docker fallback (PSQL, PG* env vars)
cd supabase/functions && npx deno@2.9.6 test --allow-env   # Edge Functions (also fmt --check, lint, check */index.ts)
terraform -chdir=infra/terraform/envs/dev init -backend=false && terraform -chdir=infra/terraform/envs/dev validate
cd services/workers && uv sync && uv run ruff check . && uv run pytest -q -m "not integration"   # workers
```

This dev machine has no Docker: use the fallback with the portable PostgreSQL 17 + PostGIS + pgTAP install (`spikes/postgres/setup-local-windows.sh`; antivirus has deleted its binaries twice — re-extract from the cached zip). CI on GitHub is the authority.

Tooling still to come: pnpm + Turborepo for TypeScript, uv for Python, pre-commit hooks for formatting, linting and secret scanning.

Frontend commands are Kimi's and live in `README.md`.
